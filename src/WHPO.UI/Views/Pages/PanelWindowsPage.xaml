<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WHPO_UI.Views.Pages.PanelWindowsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Background="{ThemeResource AppBackgroundBrush}" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="12" Margin="20,16,20,24">

            <!-- Encabezado -->
            <StackPanel Spacing="4" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <FontIcon Glyph="&#xE770;" FontSize="20" Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text="Panel de Windows" FontWeight="SemiBold" FontSize="22" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock Text="Accedé directamente a los paneles clásicos de Windows con un clic. Inspirado en la sección de paneles de WinUtil (Chris Titus Tech)."
                           FontSize="13"
                           Foreground="{ThemeResource SecondaryTextBrush}"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Cuadrícula de paneles: 3 columnas por fila (se construye desde código) -->
            <StackPanel x:Name="PanelsHost" Spacing="0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
